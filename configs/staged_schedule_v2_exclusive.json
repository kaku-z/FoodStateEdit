{
  "schema_version": "foodstateedit.staged_schedule.v2",
  "freeze_date": "2026-08-27",
  "freeze_rule": "Committed before any exclusive-mask stochastic output was generated.",
  "method": "foodstateedit_staged_exclusive",
  "parent_candidate": "configs/staged_schedule_v1.json",
  "isolated_factor": "semantic projection-mask ownership",
  "unchanged_controls": [
    "common_proxy_v1 first_frame",
    "common_proxy_v1 motion_signal",
    "common_proxy_v1 depth",
    "prompt and negative prompt",
    "Wan2.2-VACE-Fun-A14B model files",
    "seed",
    "frame count",
    "inference steps",
    "tweak index",
    "layer endpoints",
    "no-warm-start setting",
    "replace mode",
    "exact 2D protection projection"
  ],
  "seed": 1,
  "num_frames": 21,
  "num_inference_steps": 20,
  "tweak_index": 3,
  "warm_start": false,
  "replace_mode": "mask_new",
  "schedule": {
    "rigid": 18,
    "contact": 15,
    "material": 8,
    "hole": 8
  },
  "mask_mapping": {
    "rigid": "mask_rigid.png",
    "contact": "mask_contact.png",
    "material": "mask_material.png",
    "hole": "mask_hole.png"
  },
  "projection_mask_rule": {
    "ownership_priority_high_to_low": ["hole", "contact", "material", "rigid"],
    "assignment": "Each pixel in the original four-mask union is assigned to the first original mask containing it in priority order.",
    "required_invariants": [
      "pairwise projection-mask overlap is zero",
      "exclusive union equals original union",
      "every projection mask remains nonempty"
    ]
  },
  "rationale": "Source-hole repair must release early; contact owns its overlap through the middle window; material owns remaining food pixels for early texture release; rigid owns only the remaining utensil geometry.",
  "selection_policy": "One ownership rule and the unchanged v1 endpoints for all four families. No case-specific changes or output-dependent adjustment before the four-anchor review."
}
